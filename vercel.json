{
  "version": 2,
  "builds": [
    {
      "src": "api/[...all].ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "backend-lib/**",
          "backend-api/**"
        ]
      }
    },
    {
      "src": "index.html",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/[...all].ts"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
